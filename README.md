# HomeBase — Property Search

Drop these four files into a GitHub repo root and enable GitHub Pages to host a multi-API real estate search UI.

- index.html — shell & layout
- styles.css — dark/light theme
- config.js — API keys, tiers, quotas, RentCast alert
- app.js — search logic, de-dup, settings, usage tracking
